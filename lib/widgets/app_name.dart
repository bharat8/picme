import 'package:flutter/material.dart';

class AppName extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      "picme",
      textAlign: TextAlign.center,
      style: TextStyle(
          fontSize: 35, fontFamily: "Pacifico", fontWeight: FontWeight.bold),
    );
  }
}
